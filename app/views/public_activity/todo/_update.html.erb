<li class="list-group-item">
  <span class="glyphicon glyphicon-edit"></span>
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.user_name : 'Member' %></strong>
  <% if a.trackable %>
    指派了任务：<b><%= link_to(a.trackable.title, team_project_todo_path(@team, a.trackable.project,a.trackable)) %>.</b>
  <% else %>
    目前任务已被删除
  <% end %>
</li>
